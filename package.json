{
  "name": "panstwa-miasta-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run dev --prefix server",
    "install": "npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}